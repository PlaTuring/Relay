# P1-APP-001 — Production Electron workspace and locked build graph

- Owner on dispatch: Root-assigned application worker holding `ROOT-LOCKFILE` exclusively.
- Objective: create the production Electron/TypeScript workspace and immutable build graph selected by ADR-003, separate from the Phase 0 comparison spike.
- Product boundary: application scaffolding and build verification only. It must not implement inference, media generation, prompt expansion, a tool-side Generate/Run control, formal `/prompt`, automatic first-job submission or a cloud/third-party inference adapter.
- Dependencies: Root-accepted P0-ARC-005 and QA-001; accepted ADR-001/002/003/004 and ADR-011 are binding inputs.
- Allowed writes: `apps/control-plane/**`, `docs/evidence/PRODUCTION_APP_SCAFFOLD.md`.
- Forbidden writes: root package/lockfile, spike projects, native helper implementation, schemas, registry, plans, installer/model/runtime/workflow code and external paths.
- Lock: `ROOT-LOCKFILE`; no other lockfile/build-graph worker may overlap even though this task uses an isolated production lockfile.
- Required scaffold: exact package versions and lock; separated main/preload/renderer/test/build trees; sandbox/context isolation/no Node integration; closed placeholder IPC registry; restrictive CSP/navigation/permission policy; no updater/publish channel/runtime fetch; deterministic ASAR/fuse/package input inventory; SBOM and license hooks; Windows Unicode/space path test fixture. Placeholders may expose no generic command, filesystem, network, queue or generation authority.
- Supply-chain boundary: use only reviewed pinned inputs already available to the task. Network install, mutable resolution, copying opaque spike outputs and claiming an offline/reproducible release are forbidden. If exact inputs are unavailable, record `blocked_external` rather than weakening locks.
- Acceptance commands from repository root:

  ```powershell
  npm --prefix .\apps\control-plane run verify:offline
  npm test
  ```

  Expected: exit `0`; production verification reports exact dependency/build-input hashes, no forbidden capabilities, sandbox/CSP scaffold checks and public-evidence lint; root fast tests remain green. Run `verify:offline` twice with identical summary output.
- Evidence level: source/build-graph structure may become `proven`; final package, native helper, signing, VM installation, ComfyUI and H3 remain pending.
- Failure fallback: keep only the accepted spike evidence and block production app tasks; never relabel the spike as the production application.
- Unlocks after Root acceptance: P1-APP-002, P1-APP-003 and P1-NAT-004.
