# P0-CON-002 — Capability catalog and local-node allowlist schema

- Owner on dispatch: current `SCHEMA` lock holder.
- Objective: encode only proven/inferred/experimental/blocked capabilities and exact local-node identities so workflow compilation can fail closed.
- Product boundary: schema and fixtures only; no execution, download, model load, cloud/API node or `/prompt` submission.
- Dependencies: Root-accepted P0-CON-001 and P0-WF-001 upstream snapshot.
- Allowed writes: `schemas/capability-catalog/**`, `schemas/node-allowlist/**`, `tests/fixtures/contracts/capability/**`, `docs/evidence/CAPABILITY_SCHEMA.md`.
- Forbidden writes: registry, other schema families, root plan and implementation code.
- Lock: `SCHEMA`.
- Required fields/invariants: contract version/ID; immutable upstream fingerprints; evidence status and source; route/duration/prompt/endpoint/audio capabilities as separate gates; hardware/runtime/license prerequisites; exact `class_type`; input/output schema fingerprint; node origin/revision; local-only status; forbidden Partner/API/unknown identities; supersession/revocation; deterministic reason codes.
- Required fixtures: minimal valid Alpha-0 T2VA; inferred capability rejected as Stable; display-name collision; same class with schema drift; API/Partner node; unknown node; stale fingerprint; missing license/hardware gate; unknown execution-affecting field; forward-safe display metadata.
- Acceptance: one deterministic offline command validates valid fixtures and rejects each negative fixture with one exact rule; public evidence is sanitized.
- Failure fallback: compiler capability remains unavailable; never infer from a display label or loose version range.
- Unlocks: P0-CON-004, P0-CON-010, P0-CON-012 and workflow safety tasks.

## Root acceptance command

From repository root: `node .\tests\fixtures\contracts\capability\validate.mjs`. Expected exit `0`, two schema digests, two valid contracts, 17 negative cases, two valid mutations and sanitized public evidence; run twice.
