# P0-ARC-006 — Managed Core final-path layout spike

- Owner: `/root/workflow_plan_review`
- Objective: prove a managed runtime generation is built from the beginning at its final absolute path and activated only by a small atomic pointer.
- Product boundary: no generation feature is implemented; this is an install-layout and crash-safety fixture. MiniMax H3 remains the actual generator inside ComfyUI after user Run.
- Accepted inputs: `AGENTS.md`, optimized architecture, `docs/architecture/RUNTIME_TOPOLOGY_OPTIONS.md`.
- Allowed writes: `prototypes/phase0/managed-core-layout/**`, `docs/evidence/MANAGED_CORE_LAYOUT.md`.
- Locks: none; fake runtime only.
- Forbidden actions: no network, real Comfy/H3/model/GPU use, global PATH/registry/user-Python change, writes outside owned fixtures, or deletion of external data.
- Required evidence: a tiny fake runtime/Python layout works under space and Chinese paths; no populated environment moves from staging; incomplete generations never activate; success atomically replaces `active.json`; manifests/journals contain no staging reference; injected build and pre-pointer crashes recover safely.
- Acceptance: one deterministic command runs success and negative fixtures and reports exact pass/fail; owned fixture containment is checked before cleanup.
- Failure fallback: keep the incomplete generation quarantined and leave the prior/no active pointer intact.
- Unlocks after root acceptance: P0-ARC-009, P0-ARC-010 and P0-ARC-011.

## Root acceptance command

From repository root: `powershell -NoProfile -ExecutionPolicy Bypass -File .\prototypes\phase0\managed-core-layout\Invoke-ManagedCoreLayoutSpike.ps1`. Expected exit `0`, `17/17 checks passed`, and sanitized `evidence/LAST_RUN.json`; run twice.
