# P0-ARC-004 — .NET desktop bounded stack spike

- Owner on dispatch: `/root/product_risk_review` unless Root reassigns.
- Objective: evaluate a narrowly isolated Windows .NET desktop control plane against the identical Electron/Tauri Alpha comparison contract. This produces evidence only; it does not select the stack.
- Product boundary: installation, detection, verification, configuration, workflow compilation and ComfyUI handoff only. H3 inside ComfyUI generates media only after user Run.
- Accepted inputs: `AGENTS.md`, ADR-001, accepted ADR-002, accepted stack-spike reports, and `tasks/context/P0-ARC-002.md` comparison requirements.
- Allowed writes: `prototypes/phase0/stack-dotnet/**`, `docs/evidence/STACK_DOTNET.md`.
- Forbidden writes: registry, schemas, root plan/manifest/lockfiles, other prototypes.
- Locks: none. Keep project, NuGet/cache/output state isolated where the existing SDK permits. No global SDK/workload/runtime installation.
- Required evidence: exact SDK/runtime facts; framework-dependent and, if locally possible, self-contained build/package footprint; WinUI/WPF choice and Windows-version implications; native folder picker; Unicode/space path behavior; typed allowlisted UI-to-service boundary; safe argument-array owned process without shell; cancellation/termination and Job Object gap; DPAPI/credential boundary if relevant; accessibility; signing/MSIX/installer implications; updater absence; deterministic dependency/SBOM/license/security inventory.
- Comparison requirement: repeat the same fixtures, claims and resource measurements as Electron/Tauri, explicitly flagging incomparable data and shared native-helper requirements.
- Public evidence hygiene: deterministic scan must reject usernames, private `X:\\Users\\...` paths, tokens and raw environment dumps in committed reports/evidence.
- Acceptance: supported commands pass twice on existing local tools; unsupported packaging or SDK features are exact blocked results, not simulations. Report proven, inferred, blocked and production-only gates.
- Forbidden actions: no model/runtime downloads, H3/GPU, Comfy/Desktop install, cloud/API inference, updater/telemetry, global mutation or production Run/generation surface.
- Failure fallback: mark the stack conditional/unsuitable with reproducible evidence; do not install missing global prerequisites.
- Unlocks after Root acceptance: P0-ARC-005 once Electron and Tauri are also accepted.

## Root acceptance command

From repository root: `node .\prototypes\phase0\stack-dotnet\scripts\verify-static.mjs`. Expected exit `0` and deterministic summary `passed=34, blocked=14`; missing SDK/runtime/package evidence must not be promoted.
