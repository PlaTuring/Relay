# P0-ARC-011 — Generation/active-pointer crash PoC

- Owner on dispatch: Root-assigned worker.
- Objective: inject hard crashes around immutable generation materialization and active-pointer replacement, extending P0-ARC-006 from happy/fail-closed layout into recoverability evidence.
- Product boundary: fake byte artifacts only; no Comfy, Python environment, model, GPU or media generation.
- Dependency: accepted P0-ARC-006 and its owned-generation protocol.
- Allowed writes: `prototypes/phase0/generation-crash/**`, `docs/evidence/GENERATION_CRASH_POC.md`.
- Forbidden writes: registry, contracts, root plan, existing managed-core-layout fixture, user/external files.
- Locks: none. Work only under the prototype-owned temporary/work directory after resolving and checking containment.
- Required crash points: before generation create; after directory create; mid-file; after bytes before fsync/verification; before/after completion marker; before pointer temp write; after temp write; before atomic replace; immediately after replace; during retry/cleanup; parent termination with child active.
- Required invariants: incomplete generation never becomes active or launchable; old active stays valid until a fully verified replacement; pointer content is small/versioned/checksummed; retry is idempotent; quarantine/cleanup never touches external or another generation; power-loss claims are limited to what was actually simulated; Unicode/space path and volume identity retained.
- Acceptance: deterministic harness passes twice, hashes are stable, negative fixtures fail exact rules, and evidence clearly separates process-kill simulation from true power-loss/filesystem durability.
- Public evidence hygiene: no username/private absolute path, token, full environment or unbounded raw logs.
- Failure fallback: preserve last verified generation and refuse activation; never repair by guessing the newest directory.
- Unlocks after Root acceptance: P0-CON-006, P2-INS-010 and QA-014.

## Root acceptance command

From repository root: `node .\prototypes\phase0\generation-crash\scripts\run-harness.mjs`. Expected exit `0`, `19/19`, and evidence SHA-256 `e5c9ae0f445bbf09c71f311518a1d9abcde0fa87b189e32e42282e3bfd6d5da2`; run twice.
